   <% if user_signed_in? %>
        <p>The user is loged in.</p>
        <% if can? :manage, :Home %>
          <%= link_to "About", '/'   %>
        <% end %>
    <% end %>
<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
